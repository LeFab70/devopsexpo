import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-plan',
  imports: [RouterLink],
  templateUrl: './plan.html',
  styleUrl: './plan.css',
})
export class Plan {
}
