import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-phases',
  imports: [RouterLink],
  templateUrl: './phases.html',
  styleUrl: './phases.css',
})
export class Phases {
}
